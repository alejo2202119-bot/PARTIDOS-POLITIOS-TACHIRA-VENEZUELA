# ============================================================================
#  Venezuela Political Intelligence Dashboard (VPID) — backend dependencies
#  Python 3.11+. Pinned for reproducible builds.
# ============================================================================

# --- Web framework / ASGI -------------------------------------------------
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# --- Settings / validation ------------------------------------------------
pydantic==2.10.4
pydantic-settings==2.7.1

# --- Database (async SQLAlchemy 2.0 + asyncpg) ----------------------------
SQLAlchemy[asyncio]==2.0.36
asyncpg==0.30.0

# --- Auth (JWT + password hashing) ----------------------------------------
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.2.1

# --- Cache (Redis async) --------------------------------------------------
redis==5.2.1

# --- ETL / ingestion ------------------------------------------------------
httpx==0.28.1
feedparser==6.0.11

# --- Scheduling -----------------------------------------------------------
APScheduler==3.11.0

# --- PDF + charts ---------------------------------------------------------
reportlab==4.2.5
matplotlib==3.10.0

# --- Logging --------------------------------------------------------------
structlog==24.4.0

# --- Optional AI provider (sentiment fallback uses local lexicon by default)
anthropic==0.42.0

# --- Testing --------------------------------------------------------------
pytest==8.3.4
pytest-asyncio==0.25.0
